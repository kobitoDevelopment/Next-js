// "use server" を付けることで、この関数はサーバー上でのみ実行される関数になる
// クライアントコンポーネントからは直接呼び出せないため、セキュアな処理やデータ取得に適している

"use server";

export async function getFruits() {
  // 本来ここで DB から取得するなどの処理が可能
  // サンプルとしてハードコーディングされた配列を返す
  return [
    { id: 1, name: "りんご" },
    { id: 2, name: "バナナ" },
    { id: 3, name: "ぶどう" },
  ];
}

/*
 * "use server" 関数は「サーバーコンポーネント」または「アクション（form など）」からのみ呼べる。
 * fetch のようにネットワーク通信ではなく、直接呼び出すことができる。
 * 認証やデータベース読み取りなど、サーバー限定で扱いたい処理に適している。
 */
